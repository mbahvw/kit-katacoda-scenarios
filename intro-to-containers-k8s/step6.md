You can click the "DockerSite" tab to view the site and make sure it's working. Back
in your terminal, you can see our running container using the docker ps command:

`docker ps | grep hugo`{{execute}}